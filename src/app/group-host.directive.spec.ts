import { GroupHostDirective } from './group-host.directive';

describe('GroupHostDirective', () => {
  it('should create an instance', () => {
    const directive = new GroupHostDirective();
    expect(directive).toBeTruthy();
  });
});
